# Fixes Log — 2026-03-31 to 2026-04-01 time 02:15 AM

Source: `docs/implementations/platform-super-admin-implementation-plan.md` — Section 3: High Priority Gaps

---

## Pre-Phase: Fix High Priority Gaps Before Platform Expansion

### 1. Impersonation Context — Canonical Field Alignment

**File:** `src/app/providers/impersonation-context.ts`

- Added `organizationId` and `organizationSlug` as canonical fields on `ImpersonationState`
- Deprecated `tenantId` / `tenantSlug` with JSDoc annotations (kept for backward compat)
- Made `reason` **required** in `StartImpersonationInput`
- Added `organizationId` and `organizationSlug` to `StartImpersonationInput`

### 2. Impersonation Provider — DB Write Alignment

**File:** `src/app/providers/ImpersonationProvider.tsx`

- Writes `platform_super_admin_user_id` (was incorrectly `platform_admin_user_id`)
- Writes `organization_id` (NOT NULL) + `organization_slug` (required) to `impersonation_sessions`
- Keeps `tenant_id` / `tenant_slug` as compat aliases
- Added session migration logic for old storage format → canonical format
- Enriched audit metadata with `actor_type: "platform_admin"`
- Enforces reason before starting impersonation

### 3. Protected Route — Platform Admin Tenant Access Guard

**File:** `src/core/auth/components/ProtectedRoute.tsx`

- `TenantAdminRoute` now calls `useImpersonation()` for platform admin checks
- Platform admins **without** active impersonation → redirected to `/platform`
- Platform admins **with** active impersonation → allowed through to tenant routes

### 4. Tenant Slug Guard — Removed Auto-Impersonation Bootstrap

**File:** `src/core/auth/components/TenantSlugGuard.tsx`

- Removed the `useEffect` block that silently auto-started impersonation when platform admins visited `/:slug/*` routes
- Platform admins must now have a matching `organizationSlug` in their impersonation state to pass the guard
- Mismatched or missing impersonation → redirect to `/platform`

### 5. Organization Provider — Prefer ID Over Slug

**File:** `src/app/providers/OrganizationProvider.tsx`

- Impersonation org lookup now prefers `organizationId` (by ID), falls back to `organizationSlug` / `tenantSlug`
- Updated `fetchOrganizationData` dependency array to include new fields

### 6. Platform Dashboard — Controlled Impersonation Flow

**File:** `src/workspaces/platform/pages/PlatformAdminDashboard.tsx`

- Replaced direct `/:slug/app/dashboard` links with an **Impersonate button → shadcn Dialog**
- Dialog requires a reason before starting the session
- Updated route links: `/platform/tenants` → `/platform/organizations`
- Updated quick action label: "Create New Tenant" → "Manage Organizations"

### 7. Tenants Panel — Controlled Impersonation Flow

**File:** `src/workspaces/platform/pages/panels/TenantsPanel.tsx`

- Same impersonation dialog pattern as dashboard (reason required)
- Updated heading: "Tenant Management" → "Organization Management"
- Updated directory label: "Tenant Directory" → "Organization Directory"
- Updated loading/empty state text to use "organizations"

### 8. Impersonation Banner — Enhanced Context Display

**File:** `src/workspaces/platform/layout/ImpersonationBanner.tsx`

- Shows canonical `organizationSlug` (falls back to `tenantSlug`)
- Displays impersonation **reason** when available
- Uses **destructive** styling instead of warning
- Stronger exit copy: "End Impersonation Session" with `LogOut` icon

### 9. Audit Utility — Platform Action Metadata

**File:** `src/core/utils/audit.ts`

- Added optional `correlationId`, `actorType`, and `impersonationSessionId` to `AuditLogInput`
- These fields are merged into the `metadata` JSON payload on write
- Enables querying platform actions by actor type, correlation chain, and impersonation session

### 10. Background Job Script — Organization ID Fix

**File:** `scripts/enqueue-contract-expiry-alerts.mjs`

- Contracts query now selects `organization_id` alongside `tenant_id`
- Dedup check uses `organization_id` (canonical column)
- `background_jobs` insert includes `organization_id` (NOT NULL) + `tenant_id` (compat)

---

## Verification

- **TypeScript typecheck:** `npx tsc --noEmit` — ✅ Passed (zero errors)
- **Manual verification:** Platform admin cannot access tenant routes without impersonation, impersonation requires reason, banner shows org context + reason, DB writes use canonical columns

---

## Phase 1: Platform Shell & Routing Refactor

Source: `docs/implementations/platform-super-admin-implementation-plan.md` — Section 6, Phase 1

### 11. Platform Route Guard (NEW)

**File:** `src/workspaces/platform/app/PlatformRouteGuard.tsx` (NEW)

- Dedicated guard for `/platform/*` routes
- Wraps `PlatformAdminRoute` — extensible for capability-based checks in Phase 2

### 12. Platform Admin Routes (NEW)

**File:** `src/workspaces/platform/app/PlatformAdminRoutes.tsx` (NEW)

- Owns the entire `/platform/*` route tree (previously inline in App.tsx)
- Canonical routes: `organizations`, `subscriptions`, `users`, `audit-logs`, `settings`
- Legacy route redirects:
  - `/platform/tenants` → `/platform/organizations`
  - `/platform/billing` → `/platform/subscriptions`
  - `/platform/logs` → `/platform/audit-logs`
- Catch-all redirects unknown platform sub-routes to `/platform`

### 13. App.tsx — Platform Route Delegation

**File:** `src/app/App.tsx`

- Replaced inline platform route block (17 lines of `<Route>` elements) with single `<Route path="/platform/*" element={<PlatformAdminRoutes />} />`
- Removed 6 platform panel lazy imports (now owned by `PlatformAdminRoutes`)
- Removed `PlatformAdminRoute` and `PlatformAdminLayout` imports (now used only inside platform workspace)

### 14. Canonical Platform Route Helpers

**File:** `src/core/utils/routes.ts`

- Added: `platformOrganizationsPath`, `platformOrganizationDetailPath`, `platformUsersPath`, `platformUserDetailPath`, `platformSubscriptionsPath`, `platformAuditLogsPath`, `platformSettingsPath`, `platformPlansPath`, `platformModulesPath`, `platformSecurityPath`, `platformHealthPath`, `platformAnalyticsPath`

### 15. Sidebar Menu — Canonical Naming

**File:** `src/workspaces/platform/layout/PlatformAdminLayout.tsx`

- "Tenants" → "Organizations" with path `/platform/organizations`
- "Billing" → "Subscriptions" with path `/platform/subscriptions`

### 16. Users Panel — Canonical Table Reads

**File:** `src/workspaces/platform/pages/panels/UsersPanel.tsx`

- Stopped reading from `tenant_users` view (legacy compat layer)
- Now reads from `organization_memberships` joined with `organizations` and `profiles` (canonical tables)
- Updated all display labels from "tenant" to "organization"
- Added `platform_super_admin` and `org_admin` to role label map

---

## Verification (Phase 1)

- **TypeScript typecheck:** `npx tsc --noEmit` — ✅ Passed (zero errors)

### Bugfixes (found during manual testing)

#### 17. Dashboard Duplicate Key Warning

**File:** `src/workspaces/platform/pages/PlatformAdminDashboard.tsx`

- Changed React list key from `org.name` (can be duplicate) to `org.slug` (unique)

#### 18. Users Panel — 400 Bad Request

**File:** `src/workspaces/platform/pages/panels/UsersPanel.tsx`

- Removed `profile:profiles(email)` join — no FK exists between `organization_memberships` and `profiles`
- `organization_memberships` already has an `email` column — used directly instead
- Updated interface, search filter, and display to use `item.email` instead of `item.profile?.email`

---

## Phase 2: Shared Platform Infrastructure & Capability Model

Source: `docs/implementations/platform-super-admin-implementation-plan.md` — Section 6, Phase 2

Created 10 new infrastructure files under `src/workspaces/platform/shared/` to serve as reusable scaffolding for Phase 3 pages.

### 19. Capability Maps & Auth Hooks (NEW)

**Files:**
- `shared/auth/platform-capabilities.ts`: Defines `PlatformCapability` union and `DEFAULT_PLATFORM_CAPABILITIES` (currently assigning all to `platform_super_admin`).
- `shared/auth/usePlatformPermissions.ts`: Additive hook to evaluate `PlatformCapability` access, keeping existing `usePermissions` untouched.

### 20. App Hooks & Types (NEW)

**Files:**
- `shared/api/queryKeys.ts`: Singleton factory `platformKeys` for React Query caching keys.
- `shared/types/pagination.ts`: `PaginatedResult<T>`, `PaginationParams`, and offset calculators.
- `shared/utils/normalizePlatformError.ts`: Safely formats Supabase/PostgREST errors into consistent `PlatformError` shape.

### 21. Reusable Platform UI Components (NEW)

**Files:**
- `shared/components/PlatformPageHeader.tsx`: Title and action buttons.
- `shared/components/PlatformMetricCard.tsx`: Metric summary box with trend indicator.
- `shared/components/PlatformFilterBar.tsx`: Modular search + dual select filters.
- `shared/components/PlatformDangerActionDialog.tsx`: Red destructive dialog requiring string match typing.
- `shared/components/PlatformAuditMetadataDialog.tsx`: Expanding JSON tree viewer for audit logic.

### 22. Platform Route Guard Integration

**File:** `src/workspaces/platform/app/PlatformRouteGuard.tsx`

- Integrated `usePlatformPermissions` into the guard.
- Added `requiredCapability?: PlatformCapability` prop. If provided and the user lacks it, redirects to `/unauthorized`.

---

## Verification (Phase 2)

- **TypeScript typecheck:** `npx tsc --noEmit` — ✅ Passed (zero errors)

---

## Phase 3: Core Phase 1 Pages (Part A & B)

Source: `docs/implementations/platform-super-admin-implementation-plan.md` — Section 7, Phase 3

### 23. Dashboard & Analytics Engine (Part A - NEW)

**Migration:**
- `supabase/migrations/056_platform_dashboard_overview.sql` (Creates `get_platform_overview()` RPC aggregating org counts, users, failed jobs, security events, and estimated MRR securely on the server).

**Files:**
- `domains/analytics/types.ts`: Defined `PlatformOverview` and `PlatformActivityEvent`.
- `domains/analytics/api/getPlatformOverview.ts`: API using `supabase.rpc`.
- `domains/analytics/hooks/usePlatformOverview.ts`: React Query hook polling every 60s.
- `domains/analytics/components/PlatformOverviewGrid.tsx`: Mapped RPC data to `PlatformMetricCard` grid.
- `domains/analytics/components/RecentPlatformActivity.tsx`: Feed of recent org creations & impersonations from `audit_logs`.
- `pages/PlatformDashboardPage.tsx`: New canonical dashboard replacing `PlatformAdminDashboard`.

### 24. Organizations Directory (Part B - NEW)

**Files:**
- `domains/organizations/types.ts`: Defined `PlatformOrganizationRow`.
- `domains/organizations/api/listOrganizations.ts`: Paginated join across `organizations`, `organization_subscriptions`, and count of `organization_memberships`.
- `domains/organizations/hooks/usePlatformOrganizations.ts`: Hook for pagination and filters.
- `domains/organizations/components/OrganizationStatusBadge.tsx`: Reusable badge for plan & status.
- `pages/OrganizationsPage.tsx`: Built standard HTML table mapping rows. *Note: Dropped `react-table` since the dependency is not installed in `package.json`.* Replaces `TenantsPanel`.

### 25. Route Wiring

**File:** `src/workspaces/platform/app/PlatformAdminRoutes.tsx`

- Removed `PlatformAdminDashboard` -> imported `PlatformDashboardPage` mapped to `index`.
- Removed `TenantsPanel` -> imported `OrganizationsPage` mapped to `/organizations`.

---

## Verification (Phase 3 A & B)

- **TypeScript typecheck:** `npx tsc --noEmit` — ✅ Passed (zero errors)
- **Bugfix (400 Bad Request on `get_platform_overview`):** Fixed `056_platform_dashboard_overview.sql` to check `public.platform_super_admins` directly instead of querying the `user_roles` view, avoiding a role mismatch exception that PostgREST interpreted as a 400 Bad Request.
- **Bugfix (400 Bad Request on `audit_logs`):** Removed invalid `profile:profiles` join from `RecentPlatformActivity.tsx` since `audit_logs` does not have a direct foreign key to `profiles`. Made it extract email and names inline from the `metadata` JSONB column. Also fixed the schema column access by replacing `target_id` with `entity_id`.
- **Bugfix (TypeError `subs.find is not a function`):** Updated `listOrganizations.ts` to cleanly handle cases where PostgREST infers relationships as singular (Objects) instead of plural (Arrays). Added an array wrapping safeguard before calling `.find()`.

## Phase 3 (C, D, E): Users, Subscriptions, Audit Logs

- **Users Management:** Replaced legacy `UsersPanel` with `UsersPage.tsx` pointing to canonical `organization_memberships`.
- **Subscriptions & Billing:** Replaced legacy `BillingPanel` with `SubscriptionsPage.tsx` pointing to canonical `organization_subscriptions`.
- **Audit Logs:** Replaced legacy `AuditLogsPanel` with `AuditLogsPage.tsx` connected to `audit_logs` metadata.
- **Verification:** `npx tsc --noEmit` returns zero errors across the domain logic.

## Phase 4: Platform Settings & Cleanup

- **Settings Migration:** Replaced static `SettingsPanel` with a modular, capability-aware `SettingsPage.tsx`. 
- **Legacy Removal:** Deleted the deprecated `src/workspaces/platform/pages/panels/` directory. All functional logic is now exclusively in the new domain-driven components.
- **Routing:** Cleaned up `PlatformAdminRoutes.tsx` to remove all legacy laziness.
- **Final Verification:** `npx tsc --noEmit` confirmed zero broken imports across the workspace.

## Phase 5: Deep-Dive Operational Management

- **Organization Detail Page:** Implemented `OrganizationDetailPage.tsx` with a tabbed interface (Overview, Members, Billing, Audit). Provides deep-dive metrics, owner identity, and company contact metadata.
- **User Detail Page:** Implemented `UserDetailPage.tsx` providing a cross-tenant "Membership Roll-up," displaying every organization a user belongs to, their specific role, and account status.
- **Domain API Services:** Created `getOrganizationDetail.ts` and `getPlatformUserDetail.ts` to aggregate cross-repository data for administrative views.
- **Profile Resolution:** Fixed a critical bug where users appeared as "Nameless User" by improving the fallback logic in `getPlatformUserDetail.ts` to prioritize `first_name` + `last_name`, then `full_name`, then `email`.
- **Navigation Wiring:** Updated `OrganizationsPage` and `UsersPage` with contextual "View Details" actions linked to the new dynamic routes.
- **Routing:** Added lazy-loaded dynamic routes (`/organizations/:id`, `/users/:id`) to `PlatformAdminRoutes.tsx`.

## Phase 6: Advanced Subscription & Usage Management

- **Global Usage Dashboard:** Created `UsageDashboardPage.tsx` and `getGlobalUsageStats.ts` API to track storage, user, and resource consumption across the entire platform.
- **Resource Overrides:** Built the `LimitOverrideDialog.tsx` component and the `updateOrganizationLimit.ts` service to allow bespoke resource configurations for high-value customers.
- **Entitlement Management:** Implemented `ManageModulesDialog.tsx` giving the Super Admin the ability to toggle CRM, CLM, ERP, or Documents modules on or off per organization.
- **Real-Time Usage UI:** Integrated `Progress` bars in the **Organization Detail** tab, showing utilization percentages (current vs. allowed) for all tracked resources.
- **Verification:** `npx tsc --noEmit` passed cleanly for all new subscription/limit hooks.

## Fix: Phase 7 — System Health & Maintenance (2026-03-31)
**Component:** `src/workspaces/platform/domains/health` and `domains/security`
**Issue:** Platform Super Admins lacked operational visibility into background tasks and security-sensitive sessions.
**Resolution:**
- Built the **Health Domain** (`HealthPage.tsx`, `getSystemHealth.ts`) to monitor and requeue background jobs (from `background_jobs` table).
- Implemented real-time polling (every 60s) for job stats (Queued, Processing, Failed, Succeeded).
- Built the **Security Domain** (`SecurityPage.tsx`, `getSecurityOverview.ts`) to track active impersonation sessions (`impersonation_sessions` table).
- Implemented the `forceEndImpersonation` mutation allowing Super Admins to manually terminate an active session by injecting an `ended_at` timestamp.
- Expanded the audit log capabilities in `audit.ts` to support tracking `correlation_id` across multi-step platform workflows.
- Registered `/platform/health` and `/platform/security` routes.
- **Hardening:** Added database indices to `impersonation_sessions` via `057_impersonation_hardening.sql`.

---

## Phase 8: Settings & Advanced Analytics (Final Cleanup - 2026-03-31)
**Component:** `src/workspaces/platform/domains/settings` and `domains/analytics`
**Objective:** Replace static mocks with live control planes and add event telemetry.

- **Dynamic Settings (Part A):** Built `platform_settings` and `platform_feature_flags` migrations (`058`).
- **Live Governance:** Created `usePlatformSettings` hook and integrated live toggle switches and variable inputs into `SettingsPage.tsx`.
- **Telemetry Engine (Part B):** Built `platform_domain_events` ledger (`059`) for deep telemetry tracking (org velocity, event breakdown).
- **Analytics Dashboard:** Implemented `AnalyticsPage.tsx` connected to post-aggregation telemetry views.
- **Infrastructure:** Registered `/platform/analytics` and standardized `platformKeys` factory to include `analytics` and `settings`.
- **Cleanup:** 
  - Deleted legacy `src/workspaces/platform/pages/PlatformAdminDashboard.tsx`.
  - Deleted legacy `src/workspaces/platform/hooks/usePlatformDashboard.ts`.
  - Removed all remaining references to legacy `tenants` and `billing` panel logic.

**Verification:**
- `npx tsc --noEmit`: ✅ Passed (Zero errors).
- All new canonical dynamic routes (`/analytics`, `/health`, `/security`, `/settings`) are now verified as functional.
