# Fixes - 2026-04-09

**Author:** Sunny

## 1. Admin Dashboard Initial Metric Loading Delays

**Issue:**
The Organization Admin "Command Center" dashboard could surface `0` values for key metrics during the first load, creating the impression that the organization had no leads, contracts, or pending invitations.

**Root Cause:**
1. `OrganizationAdminDashboard.tsx` did not provide metric-specific skeletons during the initial hydration phase.
2. `useOrganizationAdminDashboard.ts` silently converted Supabase query failures into fallback `0` KPI values because count and list query errors were not being checked before building the dashboard payload.

**Implemented Fixes:**
1. Added dashboard skeleton placeholders for the hero, KPI cards, activity feed, chart, and table sections during the initial load.
2. Added explicit Supabase error checks inside `useOrganizationAdminDashboard.ts` so failed queries throw instead of being interpreted as valid zero metrics.
3. Enabled `keepPreviousData` behavior with a `staleTime` window so previously loaded dashboard data remains stable during refreshes.
4. Removed an unused dashboard memberships count query from the admin dashboard hook to reduce unnecessary request work.
5. Added a retry state in the admin dashboard UI for failed data synchronization.
6. Corrected the pending invitation count query to filter the `tenant_invitations` view by `tenant_id`, which matches the actual view schema.

**Files Updated:**
- `src/workspaces/organization_admin/pages/OrganizationAdminDashboard.tsx`
- `src/workspaces/organization_admin/hooks/useOrganizationAdminDashboard.ts`

**Outcome:**
The Command Center now shows loading skeletons while metrics hydrate and avoids presenting transient or error-driven `0` KPI values as if they were final data.

**Verification:**
- File lint passed:
  - `npx eslint src/workspaces/organization_admin/pages/OrganizationAdminDashboard.tsx src/workspaces/organization_admin/hooks/useOrganizationAdminDashboard.ts`
- Manual test passed:
  - Open the Command Center dashboard from a fresh load
  - Confirm skeleton placeholders appear while data is loading
  - Confirm KPI cards no longer flash misleading `0` values before the real counts arrive

---

## 2. TypeScript Config Deprecation Warning

**Issue:**
The editor reported a TypeScript configuration warning that `baseUrl` is deprecated and will stop functioning in TypeScript 7.0.

**Root Cause:**
Both `tsconfig.app.json` and the root `tsconfig.json` still declared `baseUrl`, even though the project only needs the `paths` alias mapping for `@/*`.

**Implemented Fixes:**
1. Removed `baseUrl` from `tsconfig.app.json`.
2. Removed `baseUrl` from the root `tsconfig.json`.
3. Kept the existing `paths` alias mapping intact so `@/*` imports continue to resolve correctly.
4. Removed an unused `Activity` import from `OrganizationAdminDashboard.tsx` that surfaced while validating the config cleanup.

**Files Updated:**
- `tsconfig.app.json`
- `tsconfig.json`
- `src/workspaces/organization_admin/pages/OrganizationAdminDashboard.tsx`

**Outcome:**
The `baseUrl` deprecation warning is resolved without changing the project’s import alias behavior.

**Verification:**
- File lint passed:
  - `npx eslint src/workspaces/organization_admin/pages/OrganizationAdminDashboard.tsx src/workspaces/organization_admin/hooks/useOrganizationAdminDashboard.ts`
- TypeScript validation:
  - `npm run typecheck` no longer reports the `baseUrl` deprecation issue
  - Remaining `typecheck` failures are unrelated pre-existing repo issues in other files

---

## 3. Admin Report 2.2 Status

**Issue:**
The admin debug report noted missing invitation success feedback from the `Admin Invite` entry point and the Invitations page.

**Status:**
Already fixed through the shared invitations workflow.

**Why this is already covered:**
1. The admin route `/ :tenantSlug /app/invitations` renders the shared `OrganizationInvitationsPage`.
2. The `Admin Invite` button in `AdminTopBar.tsx` navigates to that same page.
3. That shared page already uses `toast.success`, `toast.warning`, and `toast.error` from `sonner` for invitation feedback.

**Files Covered:**
- `src/workspaces/organization/pages/OrganizationInvitationsPage.tsx`
- `src/workspaces/organization_admin/components/AdminTopBar.tsx`

**Outcome:**
Admin invitation flows now inherit the same visible success feedback that was previously fixed for the owner workspace.

---

## 4. Admin Report 2.3 Navigation Delay / "Double Click" Requirement

**Issue:**
Sidebar navigation in the Organization Admin "Command Center" could feel unresponsive, especially when opening heavier module routes like CRM or CLM. The click sometimes appeared not to register until a second attempt or until the main content changed much later.

**Root Cause:**
1. The admin workspace relied on the app-level lazy-route `Suspense` boundary, so slower route loads could make the full experience feel stalled instead of showing a content-area transition immediately.
2. The admin sidebar only reflected route activation after React Router finished navigation, which left no instant visual confirmation when a link was clicked.

**Implemented Fixes:**
1. Added an admin-specific `Suspense` fallback inside `TenantAdminLayout` so route loading is handled within the main content area instead of feeling like the whole workspace has frozen.
2. Added optimistic pending-link feedback in `AdminSidebar` so the clicked destination highlights immediately while navigation is still settling.
3. Kept the short route-transition overlay in the content area so page changes feel deliberate and responsive.

**Files Updated:**
- `src/workspaces/organization_admin/layout/TenantAdminLayout.tsx`
- `src/workspaces/organization_admin/components/AdminSidebar.tsx`

**Outcome:**
Admin sidebar navigation now gives immediate visual feedback, and lazy route loading stays localized to the content region to reduce the perception that links require a second click.

**Verification:**
- File lint passed:
  - `npx eslint src/workspaces/organization_admin/components/AdminSidebar.tsx src/workspaces/organization_admin/layout/TenantAdminLayout.tsx`
- Manual test passed:
  - Open the Command Center sidebar
  - Click `CRM (All)` and `CLM (All)` once each
  - Confirm the clicked item highlights immediately and the content area shows a local loading transition instead of feeling stuck

---

## 5. Admin Report 2.4 Profile Settings Redundancy

**Issue:**
The admin profile dropdown exposed a `System Settings` shortcut that led to the same settings destination already available from the sidebar.

**Root Cause:**
The profile menu duplicated the Command Center settings entry instead of offering a distinct admin-only destination.

**Implemented Fixes:**
1. Removed the redundant `System Settings` item from the admin profile dropdown.
2. Removed the now-unused `Settings` icon import from the admin top bar component.

**Files Updated:**
- `src/workspaces/organization_admin/components/AdminTopBar.tsx`

**Outcome:**
The sidebar `Settings` link is now the single clear navigation path for admin settings, avoiding duplicate routes in the same workspace.

**Verification:**
- File lint passed:
  - `npx eslint src/workspaces/organization_admin/components/AdminTopBar.tsx`
- Manual test passed:
  - Open the admin profile dropdown
  - Confirm `System Settings` is no longer shown there
  - Confirm the sidebar `Settings` link still opens the settings page

---

## 6. CRM Report 2.1 Broken Pipeline Drag-and-Drop

**Issue:**
Dragging an opportunity card to a different stage in the CRM pipeline visually snapped the card back to its original column, making the Kanban flow feel broken.

**Root Cause:**
`OpportunityPipeline` relied entirely on the parent query result and did not maintain any local optimistic board state during a drag-and-drop mutation. The UI therefore kept rendering the old stage until the server round-trip finished.

**Implemented Fixes:**
1. Added local pipeline state inside `OpportunityPipeline` so dropped cards move immediately in the board.
2. Synced that local board state back to the latest query result whenever fresh opportunities data arrives.
3. Added rollback behavior so the card returns to its previous stage only if the update mutation actually fails.
4. Persisted `is_closed` and `is_won` alongside the stage during drag-and-drop so closed-stage moves remain internally consistent.
5. Ignored drops back into the same stage to avoid unnecessary mutations and noisy success toasts.

**Files Updated:**
- `src/modules/crm/components/OpportunityPipeline.tsx`

**Outcome:**
Pipeline cards now stay in the destination column immediately after a valid drop instead of snapping back while the mutation is in flight.

**Verification:**
- File lint passed:
  - `npx eslint src/modules/crm/components/OpportunityPipeline.tsx`
- Manual test passed:
  - Open `/crm/pipeline`
  - Drag one opportunity from `New` to `Qualified` or `Proposal`
  - Confirm the card remains in the new column immediately after dropping
  - Refresh or revisit the page and confirm the new stage persists

---

## 7. CRM Report 2.2 Opportunities Data Persistence (Close Date)

**Issue:**
The Opportunities page could show `-` in the `Close Date` column even after a close date was entered during creation, and the edit form was reading from a row shape that did not match the shared CRM opportunity model.

**Root Cause:**
1. `OpportunitiesPage` maintained its own local opportunity row type and cast the query result into it, even though the shared CRM hook already returns a different `Opportunity` shape.
2. `useCRM.ts` stored opportunity notes in `next_step`, but did not expose that value through the shared `description` field the page was using.
3. The page relied on raw `new Date(...)` parsing instead of a normalized close-date parser, which made the display path more brittle than necessary.

**Implemented Fixes:**
1. Aligned `OpportunitiesPage` to use the shared `Opportunity` type returned by `useOpportunities()` instead of a local casted row interface.
2. Updated `mapOpportunity()` to expose `description` from the persisted `next_step` value so the page and hook stay in sync.
3. Added normalized close-date parsing for both the list column and the edit dialog prefill logic.
4. Kept the opportunities list and form bound to the same shared data model so persisted values are rendered consistently after refetch.

**Files Updated:**
- `src/modules/crm/hooks/useCRM.ts`
- `src/modules/crm/pages/OpportunitiesPage.tsx`

**Outcome:**
The Opportunities screen now reads and formats persisted close dates through the shared CRM opportunity model instead of a mismatched local row shape.

**Verification:**
- File lint passed:
  - `npx eslint src/modules/crm/hooks/useCRM.ts src/modules/crm/pages/OpportunitiesPage.tsx`
- Manual test passed:
  - Open `/crm/opportunities`
  - Create a new opportunity with a close date
  - Confirm the `Close Date` column shows the saved date instead of `-`
  - Re-open the opportunity in edit mode and confirm the date is prefilled correctly

---

## 8. CRM Report 3.1 Modal Height & Action Buttons (Accounts/Contacts)

**Issue:**
The CRM account modal could grow tall enough that the primary action buttons were pushed off-screen, forcing users to scroll the whole dialog to reach `Create`. The contact modal used a similar full-height stacking pattern and benefited from the same treatment.

**Root Cause:**
Both CRM dialogs allowed the full dialog content to expand as one scrolling block instead of separating the scrollable form body from the action footer.

**Implemented Fixes:**
1. Converted the account dialog to a fixed-height modal shell with a scrollable content area.
2. Kept the dialog header and footer outside the scroll region so action buttons remain visible.
3. Applied the same layout pattern to the contact dialog for consistent CRM form behavior.
4. Added dialog padding and borders directly to the header/body/footer regions so the new structure still matches the existing visual style.

**Files Updated:**
- `src/modules/crm/pages/AccountsPage.tsx`
- `src/modules/crm/pages/ContactsPage.tsx`

**Outcome:**
CRM account and contact dialogs now keep their action buttons visible while the form content scrolls independently inside the modal.

**Verification:**
- Manual test pending:
  - Open `/crm/accounts`
  - Open `Add Account` and confirm the footer buttons remain visible while the form body scrolls
  - Open `/crm/contacts`
  - Open `Add Contact` and confirm the modal uses the same fixed-footer behavior
